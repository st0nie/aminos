#ifndef PUBLIC_H
#define PUBLIC_H
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

const int hidden_layer_number=3;
const int max_layer_unit_number=50;
const int hidden_layer_unit_number=10;
const double learning_rate=exp(-5);
const int inputs=38;
const int outputs=1;

#endif
