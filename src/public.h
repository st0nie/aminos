#ifndef PUBLIC_H
#define PUBLIC_H
#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

const int hidden_layer_number = 2;
const int max_layer_unit_number = 100;
const int hidden_layer_unit_number = 19;
const int epoch = 75;
const double learning_rate = exp(-5);
const int inputs = 38;
const int outputs = 1;
const int train_size = 2310;

#endif
